# OpenRouter deepseek/deepseek-v4-flash-0731 固定

Workers AI（kimi-k2.6 / llama-4-scout / gemma-4）は不採用。`createOpenRouter({ apiKey: env.OPENROUTER_API_KEY })` で固定IDをコードに埋め、temperature等のパラメータはデフォルト、フォールバック無し、コスト上限無し。
